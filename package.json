{
  "name": "tabghost",
  "private": true,
  "version": "0.2.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --cwd packages/session-core test",
    "api": "bun run --cwd apps/desktop start",
    "api:node": "tsx apps/desktop/src/main.ts serve",
    "dev": "bun run --cwd apps/desktop dev",
    "dev:node": "tsx --watch apps/desktop/src/main.ts serve",
    "cli": "bun run --cwd apps/desktop cli",
    "cli:node": "tsx apps/desktop/src/main.ts",
    "web": "bun run --cwd apps/control-panel dev",
    "build:core": "bun run --cwd packages/session-core build",
    "build:bin:node": "esbuild apps/desktop/src/main.ts --bundle --platform=node --external:playwright --external:playwright-core --external:bun --outfile=dist/bundled.cjs && node --experimental-sea-config sea-config.json && node -e \"require('fs').copyFileSync(process.execPath, 'dist/tabghost.exe')\" && npx postject dist/tabghost.exe NODE_SEA_BLOB dist/sea-prep.blob --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "postject": "^1.0.0-alpha.6",
    "tsx": "^4.23.0"
  }
}
