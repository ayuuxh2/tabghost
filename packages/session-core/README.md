# packages/session-core

Shared primitives for TabGhost session management.

Likely responsibilities:

- session lifecycle
- workspace isolation metadata
- proxy binding records
- deterministic replay descriptors
- state snapshots and cloning rules
